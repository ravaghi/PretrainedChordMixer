defaults:
  - _self_
  - model: chordmixer
  - loss: cross_entropy
  - optimizer: adam
  - dataset: taxonomy_classification
  - dataloader: chordmixer_dataloader
  - trainer: chordmixer_trainer
  - wandb: wandb

general:
  device_id: 0
  init_weights: true
  compute_class_weights: true
  log_every_n_steps: 100
  max_epochs: 50

hydra:
  run:
    dir: ${hydra:runtime.cwd}/../outputs
