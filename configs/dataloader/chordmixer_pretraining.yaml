_target_: dataloaders.chordmixer_pretraining.PretrainedChordMixerDataLoader
data_path: ${dataset.path}
dataset_filename: ${dataset.name}
batch_size: ${general.batch_size}
mask_ratio: 0.15
sequence_length: 10_000