defaults:
  - _self_
  - model: pretrained_chordmixer
  - loss: cross_entropy
  - optimizer: adam
  - dataset: ???
  - dataloader: pretrained_chordmixer
  - trainer: chordmixer
  - wandb: wandb

general:
  name: PretrainedChordMixer
  seed: 42
  device_id: 0
  batch_size: 2
  max_epochs: 30
  log_to_wandb: true
  use_multi_gpu: false

hydra:
  run:
    dir: ${hydra:runtime.cwd}/outputs
